# H200 量化与加速档实测（2026-08-21，`p5en.48xlarge`）

这一轮量的是**在 H200 上把 H3 跑便宜**：BF16 → FP8 → +SageAttention → +Cache-DiT，四臂 × 卡数 ×
四个几何，每个卡数档自带自己的 BF16 分母。`RESULTS.md` 那一轮回答的是「怎么把延迟压到 10 s」，
用的是更早的 sglang（`c7c03ec53b`）和 BF16；这一轮回答「每成片秒多少钱」，用
`nightly-dev-20260818-c0b6474b`。

**它顺带推翻了 `RESULTS.md` 里的一个负面结论**：Cache-DiT 在旧镜像上确实跳 0 个 block（no-op），
在 `c0b6474b` 上是正常工作的，值 1.94–2.40×。见下面「Cache-DiT」。

驱动脚本 `h200_grid.sh`，交付镜像 `Dockerfile` + `build_image.sh`，裸机准备 `h200_bringup.sh`。

## 结论

**交付方案：单卡 FP8（w8a8）**，对 BF16 快 **1.13–1.19×**，显存 78.8 → 48.1 GB（−39%）。
768p 再叠 SageAttention 值 **+5.5–5.8%**，**480p 不要叠**（比纯 FP8 慢 1.6%）。
愿意用画质换成本的话再叠 Cache-DiT，另值 **1.67–2.40×**（480p 用 `RDT=0.16`、768p 用 `RDT=0.24`，
见「Cache-DiT 的 RDT 该取多少」；下表 Cache-DiT 行统一用 0.24 以便和加卡表对齐）。

| 场景（fl2va，5.175 s 成片） | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| **BF16（分母）** | | | | |
| 1 卡 | 33.663 s | 49.515 s | 117.099 s | 175.779 s |
| 8 卡 Ulysses=8 | 5.324 s | 7.423 s | 16.446 s | 24.570 s |
| **FP8（交付）** | | | | |
| 1 卡 | **28.454 s** | **41.506 s** | 103.581 s | 155.087 s |
| 8 卡 Ulysses=8 | **4.367 s** | **6.079 s** | 14.648 s | 21.836 s |
| 对 BF16 | 1.18× | 1.19× | 1.13× | 1.13× |
| **FP8 + SageAttention** | | | | |
| 1 卡 | 28.909 s | 42.143 s | **97.918 s** | **146.295 s** |
| 8 卡 Ulysses=8 | 4.462 s | 6.186 s | **13.863 s** | **20.414 s** |
| 对纯 FP8 | 0.98× | 0.99× | **1.06×** | **1.06×** |
| **再叠 Cache-DiT `RDT=0.24`（有损，可选）** | | | | |
| 1 卡 | 14.737 s | 18.790 s | 50.473 s | 61.087 s |
| 8 卡 Ulysses=8 | 2.458 s | 2.890 s | 7.274 s | 8.679 s |
| 对同口径 FP8+sage | 1.96× | 2.24× | 1.94× | 2.40× |
| 对 BF16 | 2.28× | 2.64× | 2.32× | 2.88× |

请求口径：`short_edge` 480/768 + `aspect 16:9`、`duration 5.0`（5.175 s 成片、124 帧、24 fps）、
`flow_shift 12.0`、`audio_flow_shift 3.0`、固定 seed 6201、fl2va（参考图 = 我们自己生成的片子的首帧）。
时间是 `inference_time_s`，即整条请求（文本编码 + 采样 + VAE 解码 + 封装），全部在**同一台**
`p5en.48xlarge` 上量。前三块是**无损**的（同卡数同 seed 逐位可复现），可以互相直接比。

### 三条结论

1. **FP8 在 H200 上比在 g7e 上值钱。** 同一层优化在 g7e（RTX PRO 6000 Blackwell）上只值 1.073×，
   在 H200 上是 1.13–1.19×。480p（1.18–1.19×）比 768p（1.13×）收益更大，因为 768p 的 attention
   算力占比更高，而**权重量化碰不到 attention**。

2. **SageAttention 不移植到 sm_90。** 它在 g7e（sm_120）上值 1.26–1.29×，在 H200 上 480p 是
   **负收益**（28.909 vs 28.454，慢 1.6%），768p 只有 +5.5–5.8%。这不是编译或配置问题：镜像在
   build 时断言过 `_qattn_sm90*.so` 里有 `sm_90a` cubin（见 `Dockerfile`），服务日志里也回读到
   `Using sage_attn attention backend`。差别在**对照组**：H200 上不开 sage 时 DiT 走的是
   `fa`（FlashAttention，日志回读确认），而 g7e 上 FA 的高效路径在 sm_120 受限，sage 在那边替掉的
   是一个更弱的基线。同一份 sage kernel，对手不同，收益就不同。（更硬的归因要在两台上各做一次
   backend 消融，本轮没做。）结论：480p 的交付配置里**不放 sage**，768p 放（+5.8% 白捡，
   画质无额外损失，见「画质」）。

3. **Cache-DiT 完整移植，而且是这一轮性价比的大头。** 步数越多越值（30 步 2.24–2.40×，
   20 步 1.94–1.96×）—— 它省的是「残差变化小」的那些步，步数多的时候这种步更多。它也是唯一
   不稀释加卡收益的加速手段（768p 加卡 7.04× vs FP8+sage 的 7.06×）。

**显存：Ulysses 切的是序列不是权重，所以加卡不省显存。** 单卡峰值 BF16 78 765 MiB → FP8
48 091 MiB（+sage 47 969、+Cache-DiT 48 487）；8 卡时**每卡**反而是 BF16 ~82–83 GiB / FP8 tier
~52.0–52.5 GiB，比单卡高 4–5 GB（通信与激活缓冲）。要省显存只有 `--tp-size`（见 `RESULTS.md`）。

## 加卡（Ulysses 1 / 2 / 4 / 8）

Ulysses 在 H200 上的效率非常高，比 g7e 好得多（g7e 2 卡 768p 只有 74–75%）。1→8 的效率：

| 档 | 1 → 8 卡加速 | 效率 |
|---|---|---|
| 768p/30 FP8+sage | 7.17× | 90% |
| 768p/30 BF16 | 7.15× | 89% |
| 768p/20 FP8+sage | 7.06× | 88% |
| 768p/30 +Cache-DiT | 7.04× | 88% |
| 480p/30 FP8+sage | 6.81× | 85% |
| 480p/20 FP8+sage | 6.48× | 81% |
| 480p/20 +Cache-DiT | 6.00× | 75% |

两条规律：**分辨率越高效率越高**（序列越长，通信占比越小），**加速手段越猛效率越低**
（分子被压小了，通信这块固定开销没变）。

完整曲线（`GPUSETS="1 2 4 8"`，BF16 与 +Cache-DiT 两臂各自 1 卡分母；每格 `延迟 / 加速 / 效率 /
$每成片秒(spot)`）：

| 档 | 1 卡 | 2 卡 | 4 卡 | 8 卡 |
|---|---|---|---|---|
| **BF16** | | | | |
| 480p/20 | 33.663 s — $0.006076 | 18.978 s 1.77×/89% $0.006850 | 9.915 s 3.40×/85% $0.007158 | 5.324 s 6.32×/79% $0.007687 |
| 480p/30 | 49.515 s — $0.008937 | 27.624 s 1.79×/90% $0.009971 | 14.181 s 3.49×/87% $0.010238 | 7.423 s 6.67×/83% $0.010718 |
| 768p/20 | 117.099 s — $0.021134 | 62.746 s 1.87×/93% $0.022649 | 31.920 s 3.67×/92% $0.023044 | 16.446 s 7.12×/89% $0.023746 |
| 768p/30 | 175.779 s — $0.031725 | 93.856 s 1.87×/94% $0.033879 | 47.638 s 3.69×/92% $0.034391 | 24.570 s 7.15×/89% $0.035476 |
| **FP8+sage+Cache-DiT `RDT=0.24`** | | | | |
| 480p/20 | 14.737 s — $0.002660 | 8.641 s 1.71×/85% $0.003119 | 4.477 s 3.29×/82% $0.003232 | 2.458 s 6.00×/75% $0.003549 |
| 480p/30 | 18.790 s — $0.003391 | 10.793 s 1.74×/87% $0.003896 | 5.464 s 3.44×/86% $0.003945 | 2.890 s 6.50×/81% $0.004173 |
| 768p/20 | 50.473 s — $0.009109 | 28.150 s 1.79×/90% $0.010161 | 14.082 s 3.58×/90% $0.010166 | 7.274 s 6.94×/87% $0.010503 |
| 768p/30 | 61.087 s — $0.011025 | 34.057 s 1.79×/90% $0.012293 | 16.992 s 3.60×/90% $0.012267 | 8.679 s 7.04×/88% $0.012531 |

**768p 的 4 卡是白送的：延迟 3.6–3.7×，每成片秒和 2 卡持平**（$0.010166 vs $0.010161，
BF16 $0.023044 vs $0.022649，差 0.05%–1.7%），比 1 卡也只贵 11–12%。480p 的 4 卡贵 16–19%。
从 4 卡到 8 卡才开始付明显的钱（+3%~+10%）。**要延迟就上 4 卡，这是这台机器上最划算的一档。**

## 成本

两个口径，都用实测价：

- **`p5en.48xlarge` spot（us-east-2b）$26.8991/h = $3.36239 / 卡·小时 = $0.000933997 / 卡·秒** ——
  就是这张表跑在上面的那台。
- **客户的 `p5e` Capacity Block $47.76/h = $5.97 / 卡·小时** —— 贵 1.78×。

成片 5.175 s。**每成片秒**（fl2va）：

| 场景 | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| **spot $26.90/h** | | | | |
| FP8 1 卡 | **$0.005135** | **$0.007491** | $0.018695 | $0.027990 |
| FP8+sage 1 卡 | $0.005218 | $0.007606 | **$0.017672** | **$0.026404** |
| FP8+sage 8 卡 | $0.006443（+22%） | $0.008932（+17%） | $0.020016（+13%） | $0.029475（+12%） |
| +Cache-DiT 1 卡 | $0.002660 | $0.003391 | $0.009109 | $0.011025 |
| **Capacity Block $47.76/h** | | | | |
| FP8 1 卡 | $0.009118 | $0.013301 | $0.033193 | $0.049698 |
| FP8+sage 1 卡 | $0.009264 | $0.013505 | $0.031378 | $0.046880 |
| +Cache-DiT 1 卡 | $0.004722 | $0.006021 | $0.016174 | $0.019575 |

**每条片子**（spot、1 卡）：FP8 480p/20 $0.0266、480p/30 $0.0388、768p/20 $0.0967、768p/30 $0.1449；
叠 Cache-DiT 后 $0.0138 / $0.0176 / $0.0471 / $0.0571。

加卡溢价精确等于 `1/并行效率 − 1`，H200 上只有 **+12%~+22%**（g7e 上是 +33%~+63%），
所以「加卡买延迟不买单价」这条在 H200 上要弱得多 —— 768p 用 8 卡把延迟从 98 s 压到 13.9 s，
单价只贵 13%。

1 QPS（每秒一条请求）的机队，按 1 卡 1 副本、fl2va、FP8+sage：

| 档位 | 单卡产出 | 需要卡数 | `p5en` 台数 | spot $/h | CB $/h |
|---|---|---|---|---|---|
| 768p/30 | 1 条 / 146.3 s | 147 | 18.4 | $494 | $878 |
| 768p/20 | 1 条 / 97.9 s | 98 | 12.2 | **$330** | $585 |
| 480p/30 | 1 条 / 42.1 s | 43 | 5.4 | $145 | $257 |
| 480p/20 | 1 条 / 28.9 s | 29 | 3.6 | **$98** | $173 |
| 480p/20 +Cache-DiT | 1 条 / 14.7 s | 15 | 1.9 | **$50** | $90 |

**480p 比 768p 便宜 3.4 倍**，档位选择仍然比任何旋钮都值钱。用 DP 副本（一卡一个）而不是 Ulysses
凑吞吐：sglang 对同副本的并发请求是排队串行的。

### 和 g7e 比

同一版 sglang、同 prompt/seed/几何，各自的交付配置（H200 = FP8[+sage]，g7e = NVFP4+sage）：

| | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| H200 单卡延迟 | 28.454 s | 41.506 s | 97.918 s | 146.295 s |
| g7e 单卡延迟 | 31.125 s | 45.145 s | 114.419 s | 170.278 s |
| **H200 快** | 1.09× | 1.09× | **1.17×** | **1.16×** |
| H200 $/成片秒（spot） | $0.005135 | $0.007491 | $0.017672 | $0.026404 |
| g7e $/成片秒（3 年 SP） | $0.002990 | $0.004337 | $0.010992 | $0.016359 |
| **g7e 便宜** | 1.72× | 1.73× | **1.61×** | **1.61×** |

**H200 单卡只快 1.09–1.17×，但每卡贵 1.88×，所以每成片秒 g7e 便宜 1.61–1.73×。**
价格口径不是在偏袒 g7e：g7e 那个 3 年 SP 单价（`g7e.48xlarge` $14.31835/h）比它此刻在
us-east-2 的 spot（$14.8815/h）还贵 4%。

H200 真正买到的是**延迟的天花板**：g7e 只有 2 卡且 Ulysses 效率 74–75%，H200 有 8 卡且效率
88–90%，768p/30 能压到 20.4 s（叠 Cache-DiT 8.7 s），g7e 两卡最快是 115.4 s（叠 Cache-DiT 52.0 s）。
**要单价选 g7e，要延迟上限选 H200。** g7e 那条线在
[whn09/minimax_h3_g7e](https://github.com/whn09/minimax_h3_g7e)。

## 画质

参考 = 同几何的 **BF16 单卡**那条片（单卡同 seed 逐位可复现，所以 run-to-run 地板是 SSIM 1.0）。
除了 SSIM 还要看**帧间运动能量**：运动塌缩会把 SSIM 推**高**，只看 SSIM 会把废片判成好片。
工具 `quality_pair_local.sh`。

| 臂（1 卡） | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| BF16 8 卡（reduction 顺序地板） | 0.9691 | 0.9711 | 0.9699 | 0.9712 |
| FP8 | 0.9514 | 0.9470 | 0.9429 | 0.9418 |
| FP8+sage | 0.9480 | 0.9467 | 0.9393 | 0.9405 |
| +Cache-DiT `RDT=0.24` | 0.9309 | 0.9361 | 0.9411 | 0.9390 |

（SSIM Y。运动能量：参考 0.2172 / 0.2142 / 0.2615 / 0.2521，各臂在 0.21–0.30 之间。）

四条读法：

1. **多卡地板是 0.969–0.971，不是 1.0。** Ulysses 改的是 reduction 顺序，同 seed 也不逐位相同。
   任何低于 0.969 的差异都不能归给「多卡」。
2. **FP8 的 0.942–0.951 是真实的量化差异**（低于多卡地板），但同构图同运动，运动能量只高 3–9%。
   这是 w8a8 的正常代价。
3. **sage 不额外掉画质**：FP8+sage 和纯 FP8 在四档上差 0.0001–0.0036，在噪声里。所以 768p 那
   +5.8% 是白捡的。
4. **Cache-DiT 在 768p 几乎无损（0.939–0.941，和 FP8 同级），在 480p/20 是四格里唯一要注意的**：
   SSIM 掉到 0.9309 且运动能量涨 25%（0.2726 vs 0.2172）—— 20 步的时候 `RDT=0.24` 跳得太多。
   下一节把 RDT 扫开了。

## Cache-DiT 的 RDT 该取多少

1 卡 FP8+sage+Cache-DiT，`RDT` 扫 0.12 / 0.16 / 0.20 / 0.24（`SECONDARY_RDT` 跟着一起动），
每格 `延迟 / SSIM(Y) / 运动能量`，参考仍是同几何的 BF16 单卡（运动能量参考值
0.2172 / 0.2142 / 0.2615 / 0.2521）：

| RDT | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| 关（纯 FP8+sage） | 28.909 s / 0.9480 / 0.2386 | 42.143 s / 0.9467 / 0.2378 | 97.918 s / 0.9393 / 0.2856 | 146.295 s / 0.9405 / 0.2957 |
| 0.12 | 21.187 s / 0.9454 / 0.2393 | 22.691 s / 0.9425 / 0.2289 | 69.395 s / 0.9394 / 0.2823 | 80.031 s / 0.9436 / 0.2841 |
| **0.16** | **17.275 s / 0.9432 / 0.2401** | **20.059 s / 0.9411 / 0.2152** | 59.890 s / 0.9393 / 0.2765 | 65.823 s / 0.9410 / 0.2669 |
| 0.20 | 15.996 s / 0.9402 / 0.2501 | 18.758 s / 0.9361 / 0.2100 | 55.168 s / 0.9415 / 0.2801 | 61.050 s / 0.9390 / 0.2632 |
| 0.24 | 14.737 s / 0.9309 / 0.2726 | 18.790 s / 0.9361 / 0.2100 | **50.473 s / 0.9411 / 0.2784** | **61.087 s / 0.9390 / 0.2632** |

三条读法：

1. **768p 上 RDT 在 0.12–0.24 之间根本不掉画质**：SSIM 四档全在 0.9390–0.9415，和纯 FP8+sage 的
   0.9393/0.9405 一样，运动能量也不动。所以 768p 直接用 **0.24**（最快那档）。
2. **30 步上 RDT 0.24 和 0.20 是同一个输出**——不是相近，是 **md5 逐字节相同**
   （480p/30 和 768p/30 都是），延迟差 0.03–0.04 s 落在噪声里。步数多的时候 0.20 已经把
   「残差够小」的步全跳完了，再抬阈值也没有新的步可跳。
3. **只有 480p/20 需要往下调**：0.24 那格 SSIM 0.9309、运动能量 0.2726，是全表唯一超出 FP8 量化
   差异（0.942–0.951）的一格。降到 **0.16** 换回 SSIM 0.9432，运动能量 0.2401 也回到不开
   Cache-DiT 时的 0.2386，代价是 17.275 s 而不是 14.737 s。0.12 再往回买的画质很少（0.9454）
   但要付 21.187 s。

**推荐：480p 用 `RDT=0.16`，768p 用 `RDT=0.24`。** 这一档下的成本（1 卡，spot 口径）：

| | 480p/20 | 480p/30 | 768p/20 | 768p/30 |
|---|---|---|---|---|
| RDT | 0.16 | 0.16 | 0.24 | 0.24 |
| 延迟 | 17.275 s | 20.059 s | 50.473 s | 61.087 s |
| 对 FP8+sage | 1.67× | 2.10× | 1.94× | 2.40× |
| 对 BF16 | 1.95× | 2.47× | 2.32× | 2.88× |
| $/成片秒 | **$0.003118** | **$0.003621** | **$0.009109** | **$0.011025** |

上面「加卡」那张表里的 Cache-DiT 行用的是 `RDT=0.24`；480p 换成 0.16 后，480p 那两行的
每成片秒应按 17.275/14.737 = 1.17× 和 20.059/18.790 = 1.07× 等比抬高。

## Cache-DiT（更正 `RESULTS.md` 的负面结论）

`RESULTS.md` 里写的「Cache-DiT 在 H3 上是 no-op，跳 0 个 block、输出逐字节相同」是在
`c7c03ec53b` 上测的，**在那个镜像上是对的**（H3 的 residual 读出来是 0，所以阈值永远不触发）。
`c0b6474b` 上它正常工作。

它是 **env 驱动**的，不是 `--cache-dit-config`（那个是 diffusers 后端）：

```bash
ENVX="SGLANG_CACHE_DIT_ENABLED=1 SGLANG_CACHE_DIT_RDT=0.24 SGLANG_CACHE_DIT_SECONDARY_RDT=0.24"  # 768p
ENVX="SGLANG_CACHE_DIT_ENABLED=1 SGLANG_CACHE_DIT_RDT=0.16 SGLANG_CACHE_DIT_SECONDARY_RDT=0.16"  # 480p
```

两个坑：

- **请求里不能带 `quality` 字段** —— 带了就把这条通用 Cache-DiT 路径关掉。
- **「生效」那行日志是 warmup 时打的**（`cache-dit enabled on transformer (steps=19, Fn=1, Bn=0,
  rdt=0.240)`），和 `/health` 变绿是同一秒级。起服务后立刻回读日志会读不到，误判成没挂上。
  `h200_grid.sh` 把回读放在跑完之后。

## 部署（相对 `DEPLOYMENT_GUIDE.md` 的增量）

这一轮的镜像是**用 `Dockerfile` 烤出来的**，不再靠 `serve.sh` 在容器里做运行时改动：

```bash
setsid nohup ./h200_bringup.sh > ~/bringup.log 2>&1 < /dev/null &   # 约 40 min，主要在下 269 GiB 权重
./build_image.sh                                                    # -> h3-h200:local，约 3 min
```

四件事和 `DEPLOYMENT_GUIDE.md` 不同：

- **base 按 digest 钉死**（`lmsysorg/sglang@sha256:51e576f0…` = `nightly-dev-20260818-c0b6474b`），
  **和 g7e 那条线同一个 digest** —— 要跨平台比性价比，分子分母必须同一版 sglang。
- **AMI 是 Base OSS-driver DLAMI，不是 PyTorch DLAMI**，没有 `/opt/pytorch/bin/activate`。
  `h200_bringup.sh` 自己建 venv；顺带**先关掉 `unattended-upgrades`**（DLAMI 会在开机 20 分钟
  左右自己升包并重启，升 docker 时把正在计时的请求打掉，看起来像 spot 回收但不是）。
- **`huggingface_hub` 1.28.0 没有 `cli` / `hf_transfer` extra**：
  `pip install "huggingface_hub[hf_transfer,cli]"` 只 WARNING 然后装个没加速的 hub，
  `hf_transfer` 要当独立包装。
- **sm_90 的 SageAttention 是独立扩展**：不像 sm_120 复用 sm89 源码只多编一份 cubin，sm_90 编出
  `_qattn_sm90*.so`，所以自检断言的文件名不同。`cuobjdump --list-elf | grep sm_90a` 必须命中 ——
  pip wheel 装出来文件名一模一样、只是没有这份 cubin，运行时静默回落 Triton。
  `TORCH_CUDA_ARCH_LIST=9.0` 交叉编，建镜像**不需要 GPU**。
- **不打 NVFP4 那两个补丁**：sm_90 没有 FP4 tensor core，H200 的量化档是 FP8，走 sglang 自带的
  `--quantization fp8`，不需要离线量化文件。

起服务（单卡 480p 交付档 / 8 卡 768p 加 sage）：

```bash
IMAGE=h3-h200:local NAME=h3 GPUS=1 ULYSSES=1 \
  EXTRA="--quantization fp8 --layerwise-offload-components text_encoder" \
  WARMUP="864x480" ./serve.sh start

IMAGE=h3-h200:local NAME=h3 GPUS=8 \
  EXTRA="--quantization fp8 --attention-backend sage_attn \
    --component-attention-backends text_encoder=torch_sdpa,audio_vae=torch_sdpa,video_vae=torch_sdpa" \
  WARMUP="1344x768" ./serve.sh start
```

⚠️ **`--attention-backend` 是全局的**（上游 issue
[#35743](https://github.com/sgl-project/sglang/issues/35743)），一开 sage 就必须用
`--component-attention-backends` 把 `text_encoder` / `audio_vae` / `video_vae` 摘出去走
`torch_sdpa`，不摘 scheduler 直接死。

ref2va 另外要 `ENVX="SGLANG_MINIMAX_H3_REF_IMAGE_SHORT_EDGE=1024"`：默认 2048 把 480p/20 从
~36 s 抬到 ~65 s，而参考图短边是纯输入侧的选择。

整张表用 `./h200_grid.sh`（它自己按臂起停服务，每个卡数档自带 BF16 分母）。

## 坑

1. **`p5en` 比 `p5e` 好抢得多。** 两者都是 8×H200 141 GB，对单机 H3 等价，但 p5e 只在
   us-east-2(3 AZ)/us-west-2c/eu-north-1a 有，p5en 多了 us-east-1b/d、us-west-2a/d、
   ap-south-1a/b、ap-northeast-1a。只扫 p5e 空转 13.5 小时；加上 p5en 之后 62 秒抢到。
2. **`unattended-upgrades` 必须先关**（见「部署」）。
3. **`inference_time_s` 在 `status.json` 里的嵌套深度跟版本有关**，别写死路径（脚本里是递归找 key）。
4. **扫 RDT 时必须传 `TAGSUF=`**：tag 里没有 RDT，不传就静默覆盖同名 mp4 和 status.json，
   只剩最后一档的画质。
5. **spot 会被回收，边跑边拉结果**（`pull_results_loop.sh`）。
